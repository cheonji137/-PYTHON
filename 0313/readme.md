2장: 변수, 입력, 산술 연산 요약

변수: 데이터를 저장하고 재사용. (import math로 외외부 라이브러리 활용 가능)

입력 (input()): 사용자에게 값을 입력받음 (숫자 계산 시 int(), float()로 변환 필수).

연산 및 출력: 사칙연산(+, -, *, /)과 거듭제곱()을 사용하며, f"텍스트 {변수}" 형태로 깔끔하게 출력.

💡 한 줄 요약
input()으로 값을 입력받아 형변환 후, 변수와 산술 연산자를 이용해 계산하고 출력하는 방법.
